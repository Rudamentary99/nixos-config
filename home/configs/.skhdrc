# load scripting addintion 

# change focus
ctrl - h : yabai -m window --focus west
ctrl - j : yabai -m window --focus south
ctrl - k : yabai -m window --focus north
ctrl - l : yabai -m window --focus east


 ctrl - left  : yabai -m window --focus west
 ctrl - down  : yabai -m window --focus south
 ctrl - up    : yabai -m window --focus north
 ctrl - right : yabai -m window --focus east

# shift window in current workspace
ctrl + shift - h : yabai -m window --warp west || $(yabai -m window --display west; yabai -m display --focus west)
ctrl + shift - j : yabai -m window --warp south || $(yabai -m window --display south; yabai -m display --focus south)
ctrl + shift - k : yabai -m window --warp north || $(yabai -m window --display north; yabai -m display --focus north)
ctrl + shift - l : yabai -m window --warp east || $(yabai -m window --display east; yabai -m display --focus east)
# ctrlernatively, use the arrow keys
ctrl + shift - left : yabai -m window --swap west || $(yabai -m window --display west; yabai -m display --focus west)
ctrl + shift - down : yabai -m window --swap south || $(yabai -m window --display south; yabai -m display --focus south)
ctrl + shift - up : yabai -m window --swap north || $(yabai -m window --display north; yabai -m display --focus north)
ctrl + shift - right : yabai -m window --swap east || $(yabai -m window --display east; yabai -m display --focus east)

# set insertion point in focused container
ctrl + alt - h : yabai -m window --insert west
ctrl + alt - j : yabai -m window --insert south
ctrl + alt - k : yabai -m window --insert north
ctrl + alt - l : yabai -m window --insert east
# (ctrl) set insertion point in focused container using arrows
ctrl + alt - left  : yabai -m window --insert west
ctrl + alt - down  : yabai -m window --insert south
ctrl + alt - up    : yabai -m window --insert north
ctrl + alt - right : yabai -m window --insert east

# go back to previous workspace (kind of like back_and_forth in i3)
#ctrl - b : yabai -m space --focus recent

# move focused window to previous workspace
#ctrl + shift - b : yabai -m window --space recent; 
#                  yabai -m space --focus recent

ctrl - 1 : yabai -m space --focus 1
ctrl - 2 : yabai -m space --focus 2
ctrl - 3 : yabai -m space --focus 3
ctrl - 4 : yabai -m space --focus 4
ctrl - 5 : yabai -m space --focus 5
ctrl - 6 : yabai -m space --focus 6
ctrl - 7 : yabai -m space --focus 7
ctrl - 8 : yabai -m space --focus 8
ctrl - 9 : yabai -m space --focus 9
# #
# cmd - 1 [
#   "iterm2": yabai -m space --focus 1
#   ]
# cmd - 2 [
#   "iterm2": yabai -m space --focus 2
#   ]
# cmd - 3 [
#   "iterm2": yabai -m space --focus 3
#   ]
# cmd - 4 [
#   "iterm2": yabai -m space --focus 4
#   ]
# cmd - 5 [
#   "iterm2": yabai -m space --focus 5
#   ]
# cmd - 6 [
#   "iterm2": yabai -m space --focus 6
#   ]
# cmd - 7 [
#   "iterm2": yabai -m space --focus 7
#   ]
# cmd - 8 [
#   "iterm2": yabai -m space --focus 8
#   ]
# cmd - 9 [
#   "iterm2": yabai -m space --focus 9
#   ]
#
# move focused window to next/prev workspace
ctrl + shift - 1 : yabai -m window --space 1
ctrl + shift - 2 : yabai -m window --space 2
ctrl + shift - 3 : yabai -m window --space 3
ctrl + shift - 4 : yabai -m window --space 4
ctrl + shift - 5 : yabai -m window --space 5
ctrl + shift - 6 : yabai -m window --space 6
ctrl + shift - 7 : yabai -m window --space 7
ctrl + shift - 8 : yabai -m window --space 8
ctrl + shift - 9 : yabai -m window --space 9

# cmd + shift - 1 [
#   "iterm2": yabai -m window --space 1
#   ]
# cmd + shift - 2 [
#   "iterm2": yabai -m window --space 2
#   ]
# cmd + shift - 3 [
#   "iterm2": yabai -m window --space 3
#   ]
# cmd + shift - 4 [
#   "iterm2": yabai -m window --space 4
#   ]
# cmd + shift - 5 [
#   "iterm2": yabai -m window --space 5
#   ]
# cmd + shift - 6 [
#   "iterm2": yabai -m window --space 6
#   ]
# cmd + shift - 7 [
#   "iterm2": yabai -m window --space 7
#   ]
# cmd + shift - 8 [
#   "iterm2": yabai -m window --space 8
#   ]
# cmd + shift - 9 [
#   "iterm2": yabai -m window --space 9
#   ]
# # mirror tree y-axis
ctrl + shift - y : yabai -m space --mirror y-axis

# # mirror tree x-axis
ctrl + shift - x : yabai -m space --mirror x-axis

# balance size of windows
ctrl + shift - 0 : yabai -m space --balance

# change layout of desktop
ctrl - e : yabai -m space --layout bsp
#ctrl - l : yabai -m space --layout float
ctrl - s : yabai -m space --layout stack

# toggle float
ctrl + alt - f : yabai -m window --toggle float;\
  yabai -m window --grid 6:6:1:1:4:4

# cmd + alt - f [
#   "iterm2": yabai -m window --toggle float;\
#   yabai -m window --grid 6:6:1:1:4:4
#   ]

# cycle through stack windows
#ctrl - p : yabai -m window --focus stack.next || yabai -m window --focus south
#ctrl - n : yabai -m window --focus stack.prev || yabai -m window --focus north

# forwards
#ctrl - p : yabai -m query --spaces --space \
#            | jq -re ".index" \
#            | xargs -I{} yabai -m query --windows --space {} \
#            | jq -sre "add | map(select(.minimized != 1)) | sort_by(.display, .frame.y, .frame.x, .id) | reverse | 
#nth(index(map(select(.focused == 1))) - 1).id" \
#            | xargs -I{} yabai -m window --focus {}

ctrl - p : yabai -m window --focus stack.next || yabai -m window --focus stack.first
cmd - p : { "iterm2": yabai -m window --focus stack.next || yabai -m window --focus stack.first }
## backwards
#ctrl - n : yabai -m query --spaces --space \
#            | jq -re ".index" \
#            | xargs -I{} yabai -m query --windows --space {} \
#            | jq -sre "add | map(select(.minimized != 1)) | sort_by(.display, .frame.y, .frame.y, .id) | 
#nth(index(map(select(.focused == 1))) - 1).id" \
#            | xargs -I{} yabai -m window --focus {}

## close focused window
#ctrl - w : yabai -m window --close
## enter fullscreen mode for the focused container
#ctrl - f : yabai -m window --toggle zoom-fullscreen
#


## toggle window native fullscreen
#ctrl + shift - f : yabai -m window --toggle native-fullscreen
#a
ctrl - f10 : /usr/bin/osascript ~/Applications/toggleAudioSink 
cmd - f10 : { "iterm2": /usr/bin/osascript ~/Applications/toggleAudioSink }
alt + cmd + ctrl + shift - c : /usr/bin/osascript ~/Applications/pushToTalk.scpt
alt + cmd + ctrl - c: /opt/homebrew/bin/SwitchAudioSource -c -t input -s 'TKGOU PnP USB Microphone' -m toggle
